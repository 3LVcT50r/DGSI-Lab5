#!/usr/bin/env bash
# Wrapper for running the provider-app CLI from the workspace root on Linux/macOS.
# It changes into provider-app and sets PYTHONPATH so python can locate src.

cd "$(dirname "$0")/provider-app" || exit 1
PYTHONPATH="." python -m src.cli "$@"
